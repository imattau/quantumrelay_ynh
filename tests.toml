test_format = 1.0

[default]

    # Nostr relays are typically deployed at the domain root, but nginx
    # location-per-app packaging still works with a path.
    test_upgrade_from = []

    [default.args]
    relay_name = "Test Quantum Relay"
    require_auth = false
